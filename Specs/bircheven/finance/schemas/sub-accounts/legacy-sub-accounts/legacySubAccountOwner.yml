type: object
required:
  - id
  - subAccountId
  - createdAt
  - createdBy
properties:
  id:
    type: integer
    format: int64
  subAccountId:
    type: integer
    format: int64
  createdAt:
    type: string
    format: date-time
  createdBy:
    type: string
    format: uuid
  deletedAt:
    type: string
    format: date-time
  deletedBy:
    type: string
    format: uuid
  demandPartnerManager:
    description: List of Uuids pertaining to a Demand Partner Manager
    type: array
    items:
      type: string
      format: uuid
  demandYieldManager:
    description: List of Uuids pertaining to a Demand Yield Manager
    type: array
    items:
      type: string
      format: uuid
  supplyPartnerManager:
    description: List of Uuids pertaining to a Supply Partner Manager
    type: array
    items:
      type: string
      format: uuid
  supplyYieldManager:
    description: List of Uuids pertaining to a Supply Yield Manager
    type: array
    items:
      type: string
      format: uuid