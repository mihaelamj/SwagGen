type: object
title: Cost Per Unique Savings Click by Offer Id
required:
  - defaultCost
  - costPerOfferId
properties:
  defaultCost:
    type: number
    title: Amount (in $ USD)
    multipleOf: 0.01
    minimum: 0.01
  costPerOfferId:
    type: array
    items:
      type: object
      required:
        - key
        - value
      properties:
        key:
          type: string
          title: Offer Id
        value:
          type: number
          title: Amount (in $ USD)
          multipleOf: 0.01
          minimum: 0
allOf:
  - $ref: ../abstractRevenueClause.yml  
