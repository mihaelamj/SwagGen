type: object
required:
  - token
  - newPassword
properties:
  token:
    type: string
    description: |
      The password reset request token received by email.
  newPassword:
    type: string
    description: |
      The new password to set for the user corresponding to the token.
