type: object
required:
  - id
  - ledgerItemIds
  - newLedgerItemIds
  - createdAt
  - createdBy
properties:
  id:
    type: integer
    format: int64
  ledgerItemIds:
    type: array
    items:
      type: integer
      format: int64
  newLedgerItemIds:
    type: array
    items:
      type: integer
      format: int64
  createdAt:
    type: string
    format: date-time
  createdBy:
    type: string
    format: uuid
