type: object
properties:
  productTypes:
    type: array
    description: Product types in which the `Lead` is interested
    items:
      $ref: ../../core/schemas/productType.yml
  uuid:
    type: string
    format: uuid
    description: Primary UUID for a `Lead`
  sessionUuid:
    type: string
    format: uuid
    description: UUID used to link leads across verticals, unique per user per supply sub-account
  loanInformation:
    $ref: ./loanInformation.yml
  personalInformation:
    $ref: ./personalInformation.yml
  mortgageInformation:
    $ref: ./mortgageInformation.yml
  creditCardInformation:
    $ref: ./creditCardInformation.yml
  savingsInformation:
    $ref: ./savingsInformation.yml
  creditInformation:
    $ref: ./creditInformation.yml
  financialInformation:
    $ref: ./financialInformation.yml
  employmentInformation:
    $ref: ./employmentInformation.yml
  legalInformation:
    $ref: ./legalInformation.yml
  educationInformation:
    $ref: ./educationInformation.yml
  coApplicantInformation:
    $ref: ./coApplicantInformation.yml
  healthInformation:
    $ref: ./healthInformation.yml
  clientTags:
    $ref: ./clientTags.yml
  sessionInformation:
    $ref: ./sessionInformation.yml
  formCompleted:
    type: boolean
    description: Whether the `Lead` has completed an application form
    deprecated: true
  referralCompanyUuid:
    type: string
    format: uuid
    description: UUID of company from which the `Lead` was referred
    deprecated: true
  trackingUuid:
    type: string
    format: uuid
    # TODO this may need to be hidden
    description: UUID for internal Even Financial tracking purposes. This field should never be set by 3rd party API consumers
    deprecated: true
example:
  $ref: ../examples/leadRequestJson.yml
