description: Invalid request body JSON format
content:
  application/json:
    schema:
      type: array
      items:
        $ref: ../../core/schemas/apiError.yml
    examples:
      missingLeadUuid:
        summary: The specified uuid was not a UUID
        value:
          [
            {
              "attribute": "uuid",
              "type": "notUuid",
              "message": "uuid must be a uuid, but is 123",
              "details": {
                "specified": "123"
              }
            }
          ]
