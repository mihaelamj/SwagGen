type: object
required:
  - questions
  - groups
properties:
  questions:
    type: array
    description: >
      A list of questions
    items:
      $ref: ./question.yml
  groups:
    type: array
    description: >
      The order to display the questions and their grouping
    items:
      $ref: ./questionGroup.yml
example:
  $ref: ../examples/questionList.yml 
