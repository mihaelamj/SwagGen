//
// Generated by SwagGen
// https://github.com/yonaskolb/SwagGen
//

import Foundation

extension Rocket.Account {

    /** Create a new profile under the active account. */
    public enum CreateProfile {

        public static let service = APIService<Response>(id: "createProfile", tag: "account", method: "POST", path: "/account/profiles", hasBody: true, securityRequirements: [SecurityRequirement(type: "accountAuth", scopes: ["Catalog"])])

        public final class Request: APIRequest<Response> {

            public var body: EvenProfileCreationRequest

            public init(body: EvenProfileCreationRequest, encoder: RequestEncoder? = nil) {
                self.body = body
                super.init(service: CreateProfile.service) { defaultEncoder in
                    return try (encoder ?? defaultEncoder).encode(body)
                }
            }
        }

        public enum Response: APIResponseValue, CustomStringConvertible, CustomDebugStringConvertible {
            public typealias SuccessType = EvenProfileDetail

            /** Details of the created profile. */
            case status201(EvenProfileDetail)

            /** Bad request. */
            case status400(EvenServiceError)

            /** Invalid access token. */
            case status401(EvenServiceError)

            /** Forbidden. */
            case status403(EvenServiceError)

            /** Not found. */
            case status404(EvenServiceError)

            /** Internal server error. */
            case status500(EvenServiceError)

            /** Service error. */
            case defaultResponse(statusCode: Int, EvenServiceError)

            public var success: EvenProfileDetail? {
                switch self {
                case .status201(let response): return response
                default: return nil
                }
            }

            public var failure: EvenServiceError? {
                switch self {
                case .status400(let response): return response
                case .status401(let response): return response
                case .status403(let response): return response
                case .status404(let response): return response
                case .status500(let response): return response
                case .defaultResponse(_, let response): return response
                default: return nil
                }
            }

            /// either success or failure value. Success is anything in the 200..<300 status code range
            public var responseResult: APIResponseResult<EvenProfileDetail, EvenServiceError> {
                if let successValue = success {
                    return .success(successValue)
                } else if let failureValue = failure {
                    return .failure(failureValue)
                } else {
                    fatalError("Response does not have success or failure response")
                }
            }

            public var response: Any {
                switch self {
                case .status201(let response): return response
                case .status400(let response): return response
                case .status401(let response): return response
                case .status403(let response): return response
                case .status404(let response): return response
                case .status500(let response): return response
                case .defaultResponse(_, let response): return response
                }
            }

            public var statusCode: Int {
                switch self {
                case .status201: return 201
                case .status400: return 400
                case .status401: return 401
                case .status403: return 403
                case .status404: return 404
                case .status500: return 500
                case .defaultResponse(let statusCode, _): return statusCode
                }
            }

            public var successful: Bool {
                switch self {
                case .status201: return true
                case .status400: return false
                case .status401: return false
                case .status403: return false
                case .status404: return false
                case .status500: return false
                case .defaultResponse: return false
                }
            }

            public init(statusCode: Int, data: Data, decoder: ResponseDecoder) throws {
                switch statusCode {
                case 201: self = try .status201(decoder.decode(EvenProfileDetail.self, from: data))
                case 400: self = try .status400(decoder.decode(EvenServiceError.self, from: data))
                case 401: self = try .status401(decoder.decode(EvenServiceError.self, from: data))
                case 403: self = try .status403(decoder.decode(EvenServiceError.self, from: data))
                case 404: self = try .status404(decoder.decode(EvenServiceError.self, from: data))
                case 500: self = try .status500(decoder.decode(EvenServiceError.self, from: data))
                default: self = try .defaultResponse(statusCode: statusCode, decoder.decode(EvenServiceError.self, from: data))
                }
            }

            public var description: String {
                return "\(statusCode) \(successful ? "success" : "failure")"
            }

            public var debugDescription: String {
                var string = description
                let responseString = "\(response)"
                if responseString != "()" {
                    string += "\n\(responseString)"
                }
                return string
            }
        }
    }
}
