//
// Generated by SwagGen
// https://github.com/yonaskolb/SwagGen
//

import Foundation

public class EvenPaginationAuth: APIModel {

    /** The token type required to load the list. */
    public enum EvenType: String, Codable, Equatable, CaseIterable {
        case userAccount = "UserAccount"
        case userProfile = "UserProfile"
        case undecodable

        public init(from decoder: Decoder) throws {
            let container = try decoder.singleValueContainer()
            let rawValue = try container.decode(String.self)
            self = EvenType(rawValue: rawValue) ?? .undecodable
        }
    }

    /** The token scope required. */
    public enum EvenScope: String, Codable, Equatable, CaseIterable {
        case catalog = "Catalog"
        case commerce = "Commerce"
        case settings = "Settings"
        case undecodable

        public init(from decoder: Decoder) throws {
            let container = try decoder.singleValueContainer()
            let rawValue = try container.decode(String.self)
            self = EvenScope(rawValue: rawValue) ?? .undecodable
        }
    }

    /** The token type required to load the list. */
    public var type: EvenType

    /** The token scope required. */
    public var scope: EvenScope

    public init(type: EvenType, scope: EvenScope) {
        self.type = type
        self.scope = scope
    }

    public required init(from decoder: Decoder) throws {
        let container = try decoder.container(keyedBy: StringCodingKey.self)

        type = try container.decode("type")
        scope = try container.decode("scope")
    }

    public func encode(to encoder: Encoder) throws {
        var container = encoder.container(keyedBy: StringCodingKey.self)

        try container.encode(type, forKey: "type")
        try container.encode(scope, forKey: "scope")
    }

    public func isEqual(to object: Any?) -> Bool {
      guard let object = object as? EvenPaginationAuth else { return false }
      guard self.type == object.type else { return false }
      guard self.scope == object.scope else { return false }
      return true
    }

    public static func == (lhs: EvenPaginationAuth, rhs: EvenPaginationAuth) -> Bool {
        return lhs.isEqual(to: rhs)
    }
}
